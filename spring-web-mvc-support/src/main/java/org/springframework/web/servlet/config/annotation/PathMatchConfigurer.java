package org.springframework.web.servlet.config.annotation;

/**
 * Stub shim of Spring MVC's {@code PathMatchConfigurer}.
 * Currently a no-op placeholder; actual path matching configuration
 * is handled by the framework's native mapping registry.
 */
public class PathMatchConfigurer {
}