# 3.2.x 分支适配清单

> 从 2.7.x 分支合并代码到 3.2.x 分支时，需要做以下适配。
> 3.2.x = Spring Boot 3.2.12 + Jakarta + JDK 17 + GraalVM 预备

---

## 1. POM 依赖与版本变更

### 1.1 Parent POM
```
2.7.x:  spring-boot-starter-parent 2.7.18
3.2.x:  spring-boot-starter-parent 3.2.12
```

### 1.2 Java 版本
```
2.7.x:  <java.version>8</java.version>
3.2.x:  <java.version>17</java.version>
```

### 1.3 项目版本
```
2.7.x:  <version>2.7.0</version>
3.2.x:  <version>3.2.0</version>
```

### 1.4 依赖变更

| 依赖 | 2.7.x | 3.2.x | 原因 |
|------|-------|-------|------|
| javax.servlet-api | provided, 版本显式管理 | **移除**（由 SB3 parent 管理） | Servlet API 6.0 → jakarta.servlet |
| jakarta.servlet-api | — | `6.0.0` | javax → jakarta 迁移 |
| spring-boot-admin-client | `2.7.10` | `3.2.0` | 适配 SB3 |
| springdoc-openapi-common | `1.7.0` | → `springdoc-openapi-starter-common:2.3.0` | GroupId 从 `org.springdoc` → `org.springdoc.openapi` |
| lombok | 版本显式管理 | **移除** | SB3 parent 已管理 |
| jupiter | 版本显式管理 | **移除** | SB3 parent 已管理 |
| mockito | 版本显式管理 | **移除** | SB3 parent 已管理 |
| byte-buddy | 版本显式管理 | **移除** | SB3 parent 已管理 |
| assertj | 版本显式管理 | **移除** | SB3 parent 已管理 |

### 1.5 检查清单
- [ ] Parent POM 版本升级到 `3.2.12`
- [ ] Java 版本改为 `17`
- [ ] 项目版本号改为 `3.2.0`（或递进）
- [ ] javax.servlet-api 依赖改为 jakarta.servlet-api 6.0.0
- [ ] spring-boot-admin-client 版本升到 `3.2.0`
- [ ] springdoc-openapi 改为 `springdoc-openapi-starter-common:2.3.0`
- [ ] 移除已在 SB3 parent 中管理的依赖版本声明（lombok, jupiter, mockito, byte-buddy, assertj 等）
- [ ] 检查是否有其他第三方依赖需要升版适配 Jakarta/SB3

---

## 2. javax.\* → jakarta.\* 包迁移

### 2.1 包映射

| javax | jakarta |
|-------|---------|
| `javax.servlet.*` | `jakarta.servlet.*` |
| `javax.validation.*` | `jakarta.validation.*` |
| `javax.annotation.*` | `jakarta.annotation.*` |
| `javax.servlet.http.*` | `jakarta.servlet.http.*` |
| `javax.servlet.error.exception` | `jakarta.servlet.error.exception` |

### 2.2 影响范围

所有模块中的以下文件需执行此替换：

- **spring-web-support/servlet/** — 6 个文件（`AbstractFastFailHttpServletRequest`, `AbstractFastFailHttpServletResponse`, `FilterWrapper`, `PerfHttpServletFilterChain`, `SupportWebFilterRegistry` 等）
- **spring-web-support/** 下重写的 Spring 源码（`org.springframework.web.servlet/` 下的 7+ 文件）
- **spring-boot-starter-web/autoconfigure/** — `@ConditionalOnClass` 注解中的类名
- **所有测试文件**（35+ 个文件）

### 2.3 检查清单
- [ ] 全局搜索 `javax.servlet` → 替换为 `jakarta.servlet`
- [ ] 全局搜索 `javax.validation` → 替换为 `jakarta.validation`
- [ ] 全局搜索 `javax.annotation` → 替换为 `jakarta.annotation`
- [ ] 检查 `@ConditionalOnClass(name = "javax.*")` → 改为 `jakarta.*`
- [ ] 检查属性文件中的全限定类名引用

---

## 3. Servlet API 6.0 方法变更

### 3.1 HttpServletRequest 移除的方法
| 移除的方法 | 处理方式 |
|-----------|----------|
| `getRealPath(String)` | **删除** @Override |
| `isRequestedSessionIdFromUrl()` | **删除** @Override |

### 3.2 HttpServletRequest 新增必须实现的方法
| 新增的方法 | 处理方式 |
|-----------|----------|
| `getProtocolRequestId()` | 返回 `getRequestId()` |
| `getRequestId()` | 返回 `null` 或自定义 ID |
| `getServletConnection()` | 返回空实现或 `null` |

### 3.3 HttpServletResponse 移除的方法
| 移除的方法 | 处理方式 |
|-----------|----------|
| `encodeUrl(String)` | **删除** @Override |
| `encodeRedirectUrl(String)` | **删除** @Override |
| `setStatus(int, String)` | **删除** @Override |

### 3.4 检查清单
- [ ] `AbstractFastFailHttpServletRequest` 删除 `getRealPath()`、`isRequestedSessionIdFromUrl()`
- [ ] `AbstractFastFailHttpServletRequest` 添加 `getProtocolRequestId()`、`getRequestId()`、`getServletConnection()`
- [ ] `AbstractFastFailHttpServletResponse` 删除 `encodeUrl()`、`encodeRedirectUrl()`、`setStatus(int,String)`
- [ ] `PerfHttpServletRequest` / `PerfHttpServletResponse` 同样处理
- [ ] 检查其他 Servlet 实现类可能受影响的签名

---

## 4. Spring Framework 6.x API 变更

### 4.1 HttpMethod → 从 Enum 变为 Interface

| 变更 | 代码影响 |
|------|---------|
| `HttpMethod.resolve(String)` → `HttpMethod.valueOf(String)` | `MappingRegistry.java` 中调用点 |
| `EnumSet<HttpMethod>` → `Set<HttpMethod>` | `HttpMethodMatcher.java`、`HttpBodyCodecRegistry.java` |
| `EnumSet.copyOf(...)` → `new HashSet<>(...)` | `MappingRegistry.java` 中创建集合 |
| `httpMethod.name()` → `httpMethod.toString()` | `HttpMethodMatcher.java` |
| `switch(httpMethod)` → `if-else` 链 | `OpenApiAdapter.java` |

### 4.2 getBridgedMethod() → getMethod()

Spring 6 `HandlerMethod` 移除了 `getBridgedMethod()`，统一使用 `getMethod()`。

| 文件 | 修改 |
|------|------|
| `InvokableHandlerMethod.java` | 删除 `getBridgedMethod()` 覆盖方法 |
| `MappingRegistry.java` | `mappingContext.getBridgedMethod()` → `getMethod()` |
| `ReturnValueResolverRegistry.java` | 3 处调用 |
| `SpringHandlerMethodReturnValueHandlerAdapter.java` | 1 处调用 |
| `OpenApiAdapter.java` | `ctx.getBridgedMethod()` → `getMethod()` |

### 4.3 HttpStatus → HttpStatusCode

| 类 | 修改 |
|----|------|
| `BaseWebServerHttpResponse.java` | `setStatusCode(HttpStatus)` → `setStatusCode(HttpStatusCode)` |
| `ResponseStatusExceptionResolver.java` | `ex.getStatus()` → `ex.getStatusCode().value()` 然后 `HttpStatus.resolve()` |
| `InvokableHandlerMethod.java` | `getResponseStatus()` 返回 `HttpStatusCode` |

### 4.4 ServerHttpResponse 接口新增方法（如有用到）

Spring 6 `ServerHttpResponse` 可能新增了方法，需检查接口实现完整性。

### 4.5 检查清单
- [ ] 全局搜索 `HttpMethod.resolve(` → 改为 `HttpMethod.valueOf(`
- [ ] 全局搜索 `EnumSet<HttpMethod>` → 改为 `Set<HttpMethod>`
- [ ] 全局搜索 `EnumSet.copyOf` 涉及 HttpMethod 的 → 改为 `new HashSet<>`
- [ ] 全局搜索 `.name()` 涉及 HttpMethod 的 → 评估改为 `.toString()`
- [ ] 全局搜索 `switch` 涉及 HttpMethod 的 → 改为 if-else
- [ ] 全局搜索 `getBridgedMethod()` → 改为 `getMethod()`
- [ ] 全局搜索 `getBridgedMethod` 的覆盖声明 → 删除
- [ ] 全局搜索 `HttpStatus` 在 `setStatusCode` 中的使用
- [ ] 全局搜索 `getStatus()` 在 `ResponseStatusException` 上的调用

---

## 5. Spring Boot 3.x 自动配置机制

### 5.1 AutoConfiguration.imports

Spring Boot 3.x 不再扫描 `spring.factories` 中的 `EnableAutoConfiguration` 配置。

**新增文件**：
```
META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports
```

**内容格式**（每行一个全限定类名）：
```
io.springperf.web.autoconfigure.SpringWebAutoConfiguration
io.springperf.web.autoconfigure.SpringWebSupportAutoConfiguration
...
```

### 5.2 spring.factories 裁剪

`spring.factories` 中的 `EnableAutoConfiguration` 条目全部移除，仅保留非 auto-configuration 条目（如 `ApplicationContextFactory`）。

### 5.3 影响模块

- `spring-boot-starter-web` — 6 个 AutoConfiguration 类移入 `.imports` 文件
- `spring-web-websocket` — 新增 `WebSocketAutoConfiguration` 的 `.imports` 文件

### 5.4 检查清单
- [ ] `spring.factories` 中移除所有 `EnableAutoConfiguration` 条目
- [ ] 创建（或更新）`AutoConfiguration.imports` 文件，填入所有 auto-config 类
- [ ] 如有新模块增加 auto-configuration，需要添加对应的 `.imports` 文件
- [ ] 保留 `spring.factories` 中非 `EnableAutoConfiguration` 的条目

---

## 6. GraalVM native-image 支持

### 6.1 新建 native-image 配置文件

目录：`META-INF/native-image/io.github.springperf/spring-boot-starter-web/`

| 文件 | 内容 | 注意 |
|------|------|------|
| `reflect-config.json` | 所有框架类的反射配置 | ~353 行，需要反射的类 + 构造器/方法/字段 |
| `proxy-config.json` | 动态代理接口 | 11 个接口（包括 Spring Data、HttpMessageConverter 等） |
| `resource-config.json` | 资源模式 | spring 配置资源路径 |

### 6.2 代码层面的兼容

| 位置 | 变更 |
|------|------|
| `FastInvokerGenerator.java` | GraalVM 环境下抛出 `UnsupportedOperationException`，回退到 `MethodHandleInvoker` |
| `InvokableHandlerMethod.java` | 检测 native-image 环境，跳过 `FastInvokerGenerator` |

### 6.3 native-image 配置的维护

当新增框架组件类时，如果它被以下方式使用，需要加入 `reflect-config.json`：
- 通过反射创建实例（如 `WebComponentContainer` 中的工厂方法）
- 通过反射调用方法
- 通过反射访问字段

当新增动态代理接口时，需要加入 `proxy-config.json`。

### 6.4 验证测试

`GraalVmNativeImageConfigTest.java` 验证所有配置中的类确实存在于 classpath。

### 6.5 检查清单
- [ ] 新增组件类 → 检查是否需要加入 `reflect-config.json`
- [ ] 新增动态代理接口 → 加入 `proxy-config.json`
- [ ] 新增资源文件 → 检查是否需要加入 `resource-config.json`
- [ ] 运行 `GraalVmNativeImageConfigTest` 验证配置完整性
- [ ] 代码中涉及反射/字节码生成的地方需添加 GraalVM 兼容判断

---

## 7. 虚拟线程支持

### 7.1 线程池适配

`BizPoolRegistry.java`：
- `Map<String, ThreadPoolExecutor>` → `Map<String, ExecutorService>`
- 新增 `registerExecutor(String name, ExecutorService)` 方法
- 新增虚拟线程池创建逻辑（JDK 21+，通过反射 `Thread.ofVirtual()`）
- 根据 `spring.threads.virtual.enabled` 配置切换

### 7.2 测试

`VirtualThreadE2ETest.java` — 验证虚拟线程启用时的行为，需要 JDK 21+ 环境。

### 7.3 检查清单
- [ ] 线程池注册接口改为 `ExecutorService` 而非 `ThreadPoolExecutor`
- [ ] 新增虚拟线程创建逻辑（反射方式兼容 JDK 17/21）
- [ ] 虚拟线程相关测试增加 `assumeTrue` JDK 版本守卫（JDK 21+）

---

## 8. Springdoc OpenAPI 2.x 变更

### 8.1 依赖变更
```
org.springdoc:springdoc-openapi-common:1.7.0
  → org.springdoc.openapi:springdoc-openapi-starter-common:2.3.0
```

### 8.2 API 变更

| 2.7.x | 3.2.x |
|-------|-------|
| `OpenApiCustomiser` | `OpenApiCustomizer`（注意拼写变化） |

### 8.3 检查清单
- [ ] GroupId 改为 `org.springdoc.openapi`（注意不是 `org.springdoc`）
- [ ] ArtifactId 改为 `springdoc-openapi-starter-common`
- [ ] `OpenApiCustomiser` → `OpenApiCustomizer`
- [ ] 更新 `spring.factories` 中 springdoc 配置条目

---

## 9. 测试代码适配

### 9.1 常规替换
- 所有 `javax.servlet` import → `jakarta.servlet`
- 所有 `javax.validation` import → `jakarta.validation`
- 所有 `javax.annotation` import → `jakarta.annotation`
- `OpenApiCustomiser` → `OpenApiCustomizer`

### 9.2 WebEnvironment 配置

部分测试在 SB3 下需要显式设置 `webEnvironment`：
```java
@SpringBootTest(classes = TestApplication.class,
                webEnvironment = SpringBootTest.WebEnvironment.DEFINED_PORT)
```

### 9.3 JDK 9+ API 可用

由于基线已升级到 JDK 17，以下 API 不再需要工具类或自行实现：
- `request.getInputStream().readAllBytes()`（JDK 9+）
- `String.strip()` / `String.repeat()` / `List.copyOf()` / `Set.of()` 等

### 9.4 检查清单
- [ ] 全局搜索并替换 javax.\* import 在测试代码中
- [ ] 检查 `@SpringBootTest` 是否需要补充 `webEnvironment`
- [ ] 利用 JDK 17 简化部分工具类代码
- [ ] 检查测试中硬编码的 Servlet API 类型引用（`DispatcherType`、`ServletException` 等）

---

## 10. 合并通用流程 Checklist

每次从 2.7.x 分支合并代码到 3.2.x 时，按顺序执行：

```
□ 1. 执行合并，解决 Git 冲突
□ 2. POM 检查
    □ Parent POM 版本正确？
    □ 新增依赖是否使用了 jakarta 坐标？
    □ 新依赖版本是否由 SB3 parent 管理？
□ 3. 全局搜索替换 javax.*
    □ javax.servlet → jakarta.servlet
    □ javax.validation → jakarta.validation
    □ javax.annotation → jakarta.annotation
□ 4. API 变更检查
    □ 搜索 getBridgedMethod() → 改为 getMethod()
    □ 搜索 HttpMethod 的 EnumSet/Enum/switch 用法
    □ 搜索 HttpStatus 在 setStatusCode 中的用法
    □ 搜索 getStatus() 在 ResponseStatusException 上的调用
□ 5. 自动配置检查
    □ 新增了 auto-configuration 类？→ 加入 AutoConfiguration.imports
    □ spring.factories 中新增了 EnableAutoConfiguration？→ 移到 .imports
□ 6. GraalVM 兼容检查
    □ 新增了反射调用的类？→ 加入 reflect-config.json
    □ 新增了动态代理？→ 加入 proxy-config.json
    □ 新增了字节码生成？→ 添加 GraalVM 守卫
□ 7. 构建 & 测试
    □ mvn install -DskipTests 编译通过
    □ 运行单元测试
    □ 运行集成测试
□ 8. 编译输出检查
    □ 确保 Java 17 语法兼容
    □ 确保无 javax.* 引用残留
```

---

## 附：已适配的文件清单（commit 6e45020）

以下是在初始适配中已修改的文件，后续合并时需要关注这些文件的冲突：

**spring-boot-starter-web：**
- `pom.xml`
- `SpringWebAutoConfiguration.java` — `@ConditionalOnClass` javax→jakarta
- `OpenApiAutoConfiguration.java` — `OpenApiCustomiser`→`OpenApiCustomizer`
- `OpenApiAdapter.java` — getBridgedMethod, switch→if-else
- `spring.factories` — 裁剪 EnableAutoConfiguration
- `AutoConfiguration.imports` — 新建
- `native-image/reflect-config.json` — 新建
- `native-image/proxy-config.json` — 新建
- `native-image/resource-config.json` — 新建
- `GraalVmNativeImageConfigTest.java` — 新建

**spring-web（核心框架）：**
- `BizPoolRegistry.java` — ExecutorService + 虚拟线程
- `InvokableHandlerMethod.java` — 移除 getBridgedMethod, GraalVM 守卫, HttpStatusCode
- `FastInvokerGenerator.java` — GraalVM 守卫
- `HttpMethodMatcher.java` — Set<HttpMethod>
- `MappingRegistry.java` — HttpMethod.valueOf, HashSet
- `BaseWebServerHttpResponse.java` — HttpStatusCode
- `NettyServerHttpRequest.java` — getMethod()
- `WebServerHttpRequest.java` — getMethodValue()
- `HttpBodyCodecRegistry.java` — Set.of
- `ResponseStatusExceptionResolver.java` — HttpStatusCode
- `ReturnValueResolverRegistry.java` — getBridgedMethod→getMethod

**spring-web-support：**
- `SpringHandlerMethodReturnValueHandlerAdapter.java` — getBridgedMethod→getMethod
- `AbstractFastFailHttpServletRequest.java` — Servlet 6.0 方法变更
- `AbstractFastFailHttpServletResponse.java` — Servlet 6.0 方法变更
- `FilterWrapper.java` — jakarta.servlet.Filter
- `PerfHttpServletFilterChain.java` — jakarta.servlet.FilterChain
- `SupportWebFilterRegistry.java` — jakarta.servlet.Filter
- `ResponseEntityExceptionHandler.java` — jakarta.servlet

**spring-web-websocket：**
- `AutoConfiguration.imports` — 新建

**测试代码：**
- 35+ 测试文件 — javax→jakarta 替换

**spring-web-test / spring-web-support-test：**
- `VirtualThreadE2ETest.java` — 新建
- `CoreFeaturesController.java` — 新增虚拟线程端点
- `CacheHttpServletRequestWrapper.java` — 移除 hutool 依赖, readAllBytes
